// Vérifier si dans le texte suivant le mot Javascript est présent:
let oldString = 'JS, ECMAScript, esm6 peuvent être considérés comme des alias de Javascript'
let newString = oldString.search("Javascript");

console.log(newString);