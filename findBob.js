// En utilisant le code écrit précédemment écrivez un nouveau programme 
// dans un fichier findBob.js qui nous affichera si l'occurence "Bob" est présente.
// La casse n'est pas importante.

const str = 'alice et bob essayent de communiquer secretement, mais eve veille'
const newString = str.search("bob");
console.log(newString);